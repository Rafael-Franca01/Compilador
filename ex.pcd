int mult(int x, int y);

int main() {
  int resultado;
  
  
  resultado = mult(5, 6);
  
  prt("O resultado de 5 * 6 e: ");
  prt(resultado);
  prt("\n");
}


int mult(int x, int y) {
  rtn x * y;
}